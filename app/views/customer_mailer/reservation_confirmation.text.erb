■　予約が完了しました　■

<%= line %>

<%= @customer.name %> 様

この度は、<%= @shop.display_name %>をご利用いただきまして、誠にありがとうございます。

以下の通り、<%= @shop.display_name %>へのご予約が完了いたしました。

ご予約日時：
<%= I18n.l(@reservation.start_time, format: :long_date) %>
<%= I18n.l(@reservation.start_time, format: :time_only) %> ~ <%= I18n.l(@reservation.end_time, format: :time_only) %>

ご予約内容の変更・キャンセルは <%= @shop.phone_number %> までご連絡ください。

<% unless @customer.reminder_permission %>
ご予約を忘れない為に、以下のアドレスをクリックして予約の通知を受け取りましょう。

<%= customer_from_callbacks_reminder_permissions_url(@encrypted_data) %>
<% end %>
