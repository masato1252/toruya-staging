<%= react_component("events/participations/new", {
  props: {
    event_slug: @event.slug,
    event_title: @event.title,
    action_url: event_participation_path(event_slug: @event.slug, format: "json"),
    back_url: event_path(slug: @event.slug)
  }
}, { camelize_props: false }) %>
