<%= content_for :meta_tags do %>
  <meta property="og:title" content="<%= @event.title %>" />
  <meta property="og:description" content="<%= @event.description&.truncate(120) %>" />
<% end %>

<%= react_component("events/show", {
  props: {
    event: @event_hash,
    line_login_url: @current_event_line_user ? nil : event_auth_line_path(event_slug: @event.slug),
    add_friend_url: Rails.application.secrets.dig(:ja, :toruya_official_line_add_friend_url),
    current_event_path: request.url
  }
}, { camelize_props: false }) %>
