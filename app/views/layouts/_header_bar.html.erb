<header>
  <h1>
    <%= link_to "Toruya", root_path, id: "logo" %>
  </h1>
  <dl>
    <% if current_user %>
      <dd>
        <%= link_to root_path, class: "setting" do %>
          <i class="fa fa-calendar fa-4x" aria-hidden="true"></i>
          今日
        <% end %>
      </dd>
      <% if controller_name != "home" && super_user %>
      <dd>
        <%= link_to settings_user_shops_path(super_user), class: "setting" do %>
          <i class="fa fa-cog fa-4x" aria-hidden="true"></i>
          設定
        <% end %>
      </dd>
      <% end %>
      <dd>
        <%= link_to destroy_user_session_path, method: "delete", class: "setting" do %>
          <i class="fa fa-power-off fa-4x" aria-hidden="true"></i>
          <%= t("action.log_out") %>
        <% end %>
      </dd>
    <% end %>
  </dl>
</header>
