<%= render "form", {
  date: @reservation.start_time_date_part,
  start_time: @reservation.start_time_time_part,
  end_time: @reservation.end_time_time_part,
  reservation_path: shop_reservation_path(shop, @reservation),
  reservation: @reservation,
  menu_id: @reservation.menu_id
} %>
