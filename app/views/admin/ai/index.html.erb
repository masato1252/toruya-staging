<%= render "admin/features" %>
<%= form_with(url: build_by_url_admin_ai_index_path, local: true) do |f| %>
  <div class="field">
    <%= f.label :url %> <%= f.url_field :url, autofocus: true %>
  </div>

  <div class="actions">
    <%= f.submit t("common.send") %>
  </div>
<% end %>
