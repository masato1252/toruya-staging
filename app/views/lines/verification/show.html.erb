<div class="message centerize">
  <% if !@login_api_ready %>
    <h3 class="desc break-line-content"><%= t("common.login_to_watch_html") %></h3>

    <a href="<%= line_login_url(current_user.social_account, request.url, who: MessageEncryptor.encrypt(CallbacksController::SHOP_OWNER_CUSTOMER_SELF)) %>" class="btn line-button with-wording with-logo" data-method="post">
      <%= image_pack_tag "media/assets/booking/btn_login_base.png" %>
      <%= I18n.t("common.line_login_btn_word") %>
    </a>
  <% elsif !@message_api_ready %>
    Your message api setting is wrong
  <% else %>
    Done
  <% end %>
</div>
