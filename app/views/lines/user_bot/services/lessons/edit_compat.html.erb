<%= react_component("user_bot/services/lessons/edit", {
  props: {
    business_owner_id: business_owner_id,
    attribute: params[:attribute],
    pageContextPath: "/lines/user_bot/owner/#{business_owner_id}/services/#{params[:service_id]}/chapters/#{params[:chapter_id]}/lessons/#{params[:id]}/page_context?attribute=#{CGI.escape(params[:attribute].to_s)}",
    lesson: {
      online_service_id: params[:service_id].to_i,
      chapter_id: params[:chapter_id].to_i,
      id: params[:id].to_i,
    },
  }
}, { camelize_props: false }) %>
