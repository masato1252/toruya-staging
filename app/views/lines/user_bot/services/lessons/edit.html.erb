<%= react_component("user_bot/services/lessons/edit", {
  props: {
    business_owner_id: business_owner_id,
    attribute: @attribute,
    lesson: LessonSerializer.new(@lesson, { params: { is_owner: true } }).attributes_hash,
    course: CourseSerializer.new(@online_service, { params: { is_owner: true }}).attributes_hash,
    chapter_options: custom_options(@online_service.chapters),
    solutions: @online_service.solution_options
  }
},
{ camelize_props: false }
) %>
