<%= react_component("user_bot/services/episodes/custom_messages/edit", {
  props: {
    business_owner_id: business_owner_id,
    scenario: params[:scenario],
    pageContextPath: "/lines/user_bot/owner/#{business_owner_id}/services/#{params[:service_id]}/episodes/#{params[:episode_id]}/custom_messages/scenario_context?scenario=#{CGI.escape(params[:scenario].to_s)}#{%Q(&id=#{params[:id]}) if params[:id].present?}",
    message: {
      service_id: params[:episode_id].to_i,
      service_type: "Episode",
      online_service_id: params[:service_id].to_i,
      episode_id: params[:episode_id].to_i,
    },
  }
}, { camelize_props: false }) %>
