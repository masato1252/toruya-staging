<%= react_component("user_bot/services/episodes/custom_messages/edit", {
  props: {
    scenario: params[:scenario],
    episode_name: @episode.name,
    message: {
      id: @message&.id,
      service_id: @episode.id,
      service_type: @episode.class.name,
      online_service_id: @online_service.id,
      episode_id: @episode.id,
      template: @template
    }.merge!(@episode.message_template_variables(current_user))
  }
},
{ camelize_props: false }
) %>

