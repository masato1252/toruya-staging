<%= react_component("user_bot/services/custom_messages/edit", {
  props: {
    business_owner_id: business_owner_id,
    scenario: params[:scenario],
    pageContextPath: "/lines/user_bot/owner/#{business_owner_id}/services/#{params[:service_id]}/custom_messages/scenario_context?scenario=#{CGI.escape(params[:scenario].to_s)}#{%Q(&id=#{params[:id]}) if params[:id].present?}",
    message: {
      service_id: params[:service_id].to_i,
      service_type: "OnlineService",
      right_away: params[:right_away],
    },
  }
},
{ camelize_props: false }
) %>
