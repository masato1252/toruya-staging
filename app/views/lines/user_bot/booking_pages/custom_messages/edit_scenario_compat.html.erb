<%= react_component("user_bot/booking_pages/custom_messages/edit", {
  props: {
    business_owner_id: business_owner_id,
    scenario: params[:scenario],
    pageContextPath: "/lines/user_bot/owner/#{business_owner_id}/booking_pages/#{params[:booking_page_id]}/custom_messages/scenario_context?scenario=#{CGI.escape(params[:scenario].to_s)}#{%Q(&id=#{params[:id]}) if params[:id].present?}",
    message: {
      service_id: params[:booking_page_id].to_i,
      service_type: "BookingPage",
    },
  }
},
{ camelize_props: false }
) %>
