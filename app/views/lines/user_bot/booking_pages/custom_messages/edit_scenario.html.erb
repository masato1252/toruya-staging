<%= react_component("user_bot/booking_pages/custom_messages/edit", {
  props: {
    scenario: params[:scenario],
    message: {
      id: @message&.id,
      service_id: @booking_page.id,
      service_type: @booking_page.class.name,
      template: @template,
    }.merge!(Reservation.new(shop: @booking_page.shop).message_template_variables(current_user))
  }
},
{ camelize_props: false }
) %>
