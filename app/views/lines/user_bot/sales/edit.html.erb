<%= react_component("user_bot/sales/edit", {
  props: {
    sale_page: @sale_page.serializer.attributes_hash.merge(demo: false, preview: true),
    attribute: params[:attribute],
    flow_tips: I18n.t("user_bot.dashboards.sales.booking_page_creation.flow_tips"),
    staffs: current_user.staffs.includes(picture_attachment: :blob).map { |staff|
        {
        label: staff.name,
        value: StaffSerializer.new(staff).attributes_hash
      }
    },
  }
},
{ camelize_props: false }
) %>
