<%= react_component("user_bot/settings/shops/edit", {
  props: {
    shop: ShopFormSerializer.new(@shop).attributes_hash,
    attribute: params[:attribute],
    previous_path: @previous_path,
    title: @title,
    header: @header
  }
},
{ camelize_props: false }
) %>
