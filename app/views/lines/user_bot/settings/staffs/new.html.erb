<%= form_with(url: lines_user_bot_settings_staffs_path(business_owner_id: business_owner_id)) do |f| %>
  <%= f.text_field :phone_number, placeholder: Profile.human_attribute_name(:phone_number) %>
  <%= f.submit t("common.send"), class: "btn btn-tarco" %>
<% end %>
