■ Please confirm reservation(s) ■

<%= line %>

Dear <%= @user.name %>

There's a new pending reservation made from <%= @booking_page.title %>.

Please confirm following reservations.

<%= I18n.l(@reservation.start_time, format: :date_with_wday) %> ~ <%= I18n.l(@reservation.end_time, format: :time_only) %>
<%= date_member_url(@reservation.start_time.to_fs(:date), @reservation.id) %>
<%= @customer.name %> booked for <%= @booking_option.display_name %> on <%= I18n.l(@reservation_customer.booking_at, format: :date_with_wday) %>
